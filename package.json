{
  "name": "aws-cloud-club-pup-san-juan",
  "private": true,
  "version": "1.0.0",
  "description": "AWS Student Cloud Club PUP San Juan Website (frontend only)",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "lint": "npm run lint --prefix client"
  },
  "keywords": ["aws", "cloud", "club", "pup", "san-juan", "react", "node", "postgresql"],
  "author": "AWS Student Cloud Club PUP San Juan",
  "license": "MIT",
  "devDependencies": {}
}
